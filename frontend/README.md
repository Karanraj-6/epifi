# Epifi Notes Web

React + Vite frontend for the notes API.

## Environment

Copy `.env.example` to `.env`:

```bash
VITE_API_URL=http://localhost:4000
```

Use the deployed backend URL in production.

## Local Commands

```bash
npm run dev --workspace frontend
npm run build --workspace frontend
```
